<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <% if micropost.original_id.present? %>
    <span class="content">
      <%= link_to original_user(micropost).name, original_user(micropost) %>
      の記事をリツイート
    </span>
  <% end %>
  <span class="content"><%= micropost.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user == micropost.user %>
      <%= link_to "delete", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>
    <% if micropost.original_id.nil? %>
      <%= link_to "リツイートする", retweet_micropost_path(micropost), method: :post %>
    <% else %>
      リツイート記事
    <% end %>
  </span>
  <%= render 'microposts/favorite_form', micropost: micropost %>
</li>